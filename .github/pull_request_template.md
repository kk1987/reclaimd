<!-- What changes, and why. If it changes how a threshold is derived, say what
     measurement it came from. -->

- [ ] `gofmt -l .` is empty and `go vet ./...` is clean
- [ ] `go test -race ./...` passes
- [ ] `go.mod` still has no `require` block
